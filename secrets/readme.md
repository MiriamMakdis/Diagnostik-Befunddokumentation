# Secrets

Dieser Ordner enthält lokale Schlüssel für das signieren und verifizieren von jwt tokens.

Die Dateien `jwt-private.pem` und `jwt-public.pem` werden lokal erzeugt mit:

```bash
yarn keys:generate
```
im top level Verzeichnis.
